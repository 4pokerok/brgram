Place custom sounds here:

- notification.mp3 -> sound for new message notifications
- call.mp3 -> looping sound for incoming calls

If you use another format (wav/ogg), keep the same names and update paths in apps/web/src/App.tsx.
